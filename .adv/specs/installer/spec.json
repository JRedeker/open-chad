{
  "$schema": "https://raw.githubusercontent.com/anomalyco/oc-plugins/main/advance/plugin/schemas/spec.schema.json",
  "name": "installer",
  "title": "Installer",
  "purpose": "Capability: Installer",
  "version": "1.1.0",
  "updated_at": "2026-02-26T08:27:21.951Z",
  "requirements": [
    {
      "id": "rq-zsh.1",
      "title": "setup_zsh_plugins.sh installs zsh and three plugins",
      "body": "The module installs zsh via apt if absent, then clones romkatv/powerlevel10k, zsh-users/zsh-autosuggestions, and zdharma-continuum/fast-syntax-highlighting into ~/.zsh/plugins/. On subsequent runs it git-pulls each plugin instead of re-cloning.",
      "priority": "must",
      "scenarios": [
        {
          "id": "sc-zsh.1.1",
          "title": "Clean slate install",
          "given": [
            "zsh is not installed",
            "~/.zsh/plugins/ does not exist"
          ],
          "when": "bash lib/setup_zsh_plugins.sh is run",
          "then": [
            "zsh is installed via apt",
            "all three plugin directories exist under ~/.zsh/plugins/",
            "the script exits 0"
          ]
        },
        {
          "id": "sc-zsh.1.2",
          "title": "Idempotent re-run",
          "given": [
            "all three plugin directories already exist as git repos"
          ],
          "when": "bash lib/setup_zsh_plugins.sh is run a second time",
          "then": [
            "git pull is called on each plugin (no re-clone)",
            "the script exits 0",
            "no duplicate entries appear in ~/.zshrc"
          ]
        }
      ]
    },
    {
      "id": "rq-zsh.2",
      "title": "Managed block in ~/.zshrc is idempotent",
      "body": "The module writes exactly one OPEN-CHAD ZSH BEGIN/END block to ~/.zshrc. Running the module N times results in exactly one block. User content outside the block is never modified.",
      "priority": "must",
      "scenarios": [
        {
          "id": "sc-zsh.2.1",
          "title": "First run appends block",
          "given": [
            "~/.zshrc exists with user content",
            "no managed block present"
          ],
          "when": "bash lib/setup_zsh_plugins.sh is run",
          "then": [
            "~/.zshrc contains exactly one OPEN-CHAD ZSH BEGIN/END block",
            "all pre-existing user content is preserved verbatim"
          ]
        },
        {
          "id": "sc-zsh.2.2",
          "title": "Second run does not duplicate block",
          "given": [
            "~/.zshrc already contains an OPEN-CHAD ZSH BEGIN/END block"
          ],
          "when": "bash lib/setup_zsh_plugins.sh is run again",
          "then": [
            "~/.zshrc still contains exactly one block (count=1), not two"
          ]
        }
      ]
    },
    {
      "id": "rq-zsh.3",
      "title": "Plugin sourcing order is correct",
      "body": "The managed block sources plugins in the order: powerlevel10k → zsh-autosuggestions → fast-syntax-highlighting. fast-syntax-highlighting must be last.",
      "priority": "must",
      "scenarios": [
        {
          "id": "sc-zsh.3.1",
          "title": "Correct plugin order in managed block",
          "given": [
            "setup_zsh_plugins.sh has run successfully"
          ],
          "when": "the OPEN-CHAD ZSH BEGIN/END block in ~/.zshrc is inspected",
          "then": [
            "powerlevel10k source line appears before zsh-autosuggestions",
            "zsh-autosuggestions appears before fast-syntax-highlighting"
          ]
        }
      ]
    },
    {
      "id": "rq-zsh.4",
      "title": "--skip-zsh flag skips the wizard step non-interactively",
      "body": "wizard.sh accepts --skip-zsh / SKIP_ZSH=1. install.sh accepts --skip-zsh and passes it through. In --yes mode the chsh prompt is silently skipped.",
      "priority": "must",
      "scenarios": [
        {
          "id": "sc-zsh.4.1",
          "title": "Wizard completes without hanging when --skip-zsh is set",
          "given": [
            "wizard.sh is invoked with --yes --skip-zsh and all other --skip-* flags"
          ],
          "when": "the wizard runs",
          "then": [
            "it completes in under 10 seconds without hanging",
            "setup_zsh_plugins.sh is not called"
          ]
        }
      ]
    },
    {
      "id": "rq-zsh.5",
      "title": "open-chad update re-runs setup_zsh_plugins.sh non-fatally",
      "body": "lib/update.sh calls setup_zsh_plugins.sh. If it fails, a warning is emitted but the update continues and exits 0.",
      "priority": "should",
      "scenarios": [
        {
          "id": "sc-zsh.5.1",
          "title": "Update continues on zsh setup failure",
          "given": [
            "setup_zsh_plugins.sh exits non-zero"
          ],
          "when": "open-chad update runs",
          "then": [
            "update.sh emits a WARN line",
            "update continues to completion with exit 0"
          ]
        }
      ]
    },
    {
      "id": "rq-ocRen01",
      "title": "Primary launcher command is openchad with oc alias",
      "body": "Installer and update flows publish openchad as the canonical launcher while providing oc as a forwarding alias. Legacy open-chad usage must be detected and guided with migration messaging rather than silently breaking.",
      "priority": "must",
      "scenarios": [
        {
          "id": "rq-ocRen01.1",
          "title": "Fresh install creates canonical and alias launchers",
          "given": [
            "no existing open-chad symlinks in ~/.local/bin"
          ],
          "when": "install.sh is run",
          "then": [
            "~/.local/bin/openchad exists and points to repo launcher",
            "~/.local/bin/oc exists and forwards all args to openchad",
            "cds, oc-list, and oc-killall symlinks still exist"
          ]
        },
        {
          "id": "rq-ocRen01.2",
          "title": "Legacy invocation gets migration guidance",
          "given": [
            "a user invokes open-chad after the rename"
          ],
          "when": "the command is executed",
          "then": [
            "the user receives a clear migration warning or compatibility path",
            "documentation and doctor output reference openchad as canonical"
          ]
        }
      ]
    },
    {
      "id": "rq-ocRen02",
      "title": "Symlink lifecycle is centralized and consistent",
      "body": "All symlink management must be driven by a single manifest consumed by install, update, doctor, uninstall, and installer tests to prevent drift between command sets.",
      "priority": "must",
      "scenarios": [
        {
          "id": "rq-ocRen02.1",
          "title": "Install and update use same managed symlink set",
          "given": [
            "the managed symlink manifest is defined"
          ],
          "when": "install.sh and lib/update.sh are executed independently",
          "then": [
            "both scripts create/repair the exact same symlink names",
            "no command exists in one path but not the other"
          ]
        }
      ]
    },
    {
      "id": "rq-ocRen03",
      "title": "Rename rollout is TDD-first and regression-tested",
      "body": "For rename and alias behavior, tests are authored before implementation changes and regression coverage spans install, update, shell profile wiring, and oc session helpers.",
      "priority": "should",
      "scenarios": [
        {
          "id": "rq-ocRen03.1",
          "title": "Red-to-green ordering is explicit in task graph",
          "given": [
            "implementation tasks for launcher rename and aliasing"
          ],
          "when": "task dependencies are inspected",
          "then": [
            "implementation tasks are blocked by the TDD scaffolding task",
            "no test task is blocked by implementation tasks"
          ]
        }
      ]
    }
  ]
}